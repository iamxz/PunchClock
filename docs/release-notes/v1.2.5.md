## 更新内容

本版主要是应用更名与提醒体验的重做：菜单栏和 Dock 里现在是「小打卡」。

- **应用更名为「小打卡」**：安装包与程序显示名统一使用中文名（下载文件仍为 `Daka-<版本>.pkg` / `.app.zip`）。
- **Toast 通知改为微信式堆叠**：新提示往下叠加、最多同时 4 张，每张完整不透明，不再互相遮挡。
- **提示按重要程度分级消失**：普通操作反馈 6 秒后自动收起；健康打卡与升级提醒常驻，需手动关闭。
- **升级提醒由弹窗改为常驻卡片**：不再打断操作，卡片会一直留在界面上直到你处理。
